const Pagination = (props) => {
  return (
    <div style={{ display: 'flex' }}>
      <button onClick={() => {}} disabled={true}>
        Anterior
      </button>
      <button>{'curr'}</button>
      <button onClick={() => {}} disabled={true}>
        Proxima
      </button>
    </div>
  );
};

export default Pagination;
